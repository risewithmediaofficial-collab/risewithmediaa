{
  "name": "risewithmediaa",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "deploy": "npx wrangler deploy"
  }
}
